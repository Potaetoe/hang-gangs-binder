// One sentence of bad news and a link back. It was the last page still
// shipping the client runtime, which the content policy has no reason
// to allow - and no reason to, since there is nothing here to hydrate.
export const csr = false;
