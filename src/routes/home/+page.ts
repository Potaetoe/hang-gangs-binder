// Same reasoning as the door (src/routes/+page.ts): plain forms,
// server-rendered, no hydration to fight the person filling them. The
// trend lines are server-drawn SVG, so they need no script either.
export const csr = false;
