{
	"$schema": "./node_modules/wrangler/config-schema.json",
	"name": "hang-gangs-binder",
	"compatibility_date": "2026-08-24",
	"compatibility_flags": ["nodejs_als"],
	"main": ".svelte-kit/cloudflare/_worker.js",
	"assets": {
		"binding": "ASSETS",
		"directory": ".svelte-kit/cloudflare"
	},
	"workers_dev": true,
	"preview_urls": true,
	// Crash lines ONLY (owner OK 2026-08-26, after an outage left no
	// trace to read). Invocation logs stay OFF on purpose: they would
	// store request URLs, and the Telegram sign-in carries a member's
	// name in one. With them off, the only thing Workers Logs ever
	// stores is what the app itself logs - and the app logs exactly
	// one thing: an unexpected server error's route and message
	// (hooks.server.ts handleError).
	"observability": {
		"logs": {
			"enabled": true,
			"invocation_logs": false
		}
	},
	// Brute-force defence for the sign-in and register forms. The
	// dashboard's rate-limiting rules are scoped to a DOMAIN, and this
	// site answers on workers.dev, which is not one we own - so the
	// limiting lives in the Worker instead, where it works regardless
	// (security pass, 2026-08-24). period must be 10 or 60.
	"ratelimits": [
		{
			"name": "LOGIN_LIMIT",
			"namespace_id": "1001",
			"simple": { "limit": 6, "period": 60 }
		}
	],
	"d1_databases": [
		{
			"binding": "DB",
			"database_name": "binder-db",
			"database_id": "9eb1b7e9-11a1-4329-bb7b-f770636ce3d1",
			"migrations_dir": "drizzle"
		}
	]
}
