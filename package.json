{
  "name": "hang-gangs-binder",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling only. This package is NOT the site - apps/web is copied verbatim to the published output and nothing here is bundled, transpiled, or shipped. See DESIGN.md, 'What is deliberately not here'.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0"
  }
}
